apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ include "telegraf.fullname" . }}
{{ include "common.labels" . | indent 4 }}
  name: {{ include "telegraf.fullname" . }}
  namespace: {{ dig "global" "monitoring" "namespace" .Release.Namespace (.Values | merge (dict)) | quote }}
spec:
  ports:
    - name: telegraf
      port: 8125
      targetPort: 8125
  selector:
    app: {{ include "telegraf.fullname" . }}
  clusterIP: None
